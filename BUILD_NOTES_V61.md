# Build Notes v61 - Compact Title Menu

This update keeps the v0.6.0 gameplay/story flow intact and only adjusts the title screen/menu presentation.

## Changed
- Resized the main menu into a compact lower console so the background title artwork remains visible.
- Removed the duplicated menu-card logo/title from view with CSS so the production background title is the main visual focus.
- Returned the Initialize Operator button into the real menu grid instead of leaving it as a large fixed bottom button.
- Added responsive menu sizing for desktop, laptop, short screens, and mobile.
- Bumped CSS/JS cache query strings to v61 so GitHub Pages refreshes the updated UI.

## Unchanged
- v0.6.0 Chapter Flow systems.
- Menu button IDs and JavaScript routing.
- Existing audio, battle, mission, database, and progression behavior.
