# Project: ASH VECTOR — V97 Opening Story Force Patch

This patch is a stronger test version of the opening story update.

## Files changed
- `index.html`
- `js/game.js`
- `css/style.css`
- `UPDATE_V97.md`

## Changes
- Bumped visible build to `v0.9.7`.
- Bumped CSS/JS cache strings to `?v=97`.
- New Game / Initialize Operator now forces the opening prologue every time a fresh run starts.
- Added `Replay Opening Story` to the main menu so the prologue can be tested directly.
- Added `Replay Opening Story` inside the Playtest Console.
- Kept controller-friendly story navigation.

## Upload path
Upload these files to the root of the repo, replacing the matching files:

```txt
/index.html
/js/game.js
/css/style.css
/UPDATE_V97.md
```
