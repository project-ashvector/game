# Project ASH VECTOR V281 – Mission + Progress Scroll Fix

- Directly fixed Mission Briefing so it uses one vertical scroll document instead of cramped columns.
- Directly fixed Progression Matrix so the whole matrix area scrolls down properly.
- Added JS-level inline layout repair so browser/CSS cache cannot leave the old broken grid active.
- Kept gameplay, saves, NPCs, portals, controller input, combat, rewards, and asset loading untouched.
