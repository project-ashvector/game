# Build Notes v48

Menu database buttons now hide the main menu before opening their modal, use z-index 9000, and include inline fallback handlers plus window.AV_MENU direct helpers. Escape/Close returns to the main menu when opened from menu.
