# Build Notes V51 — Map Art Fix

- Removed `assets/custom/` generated concept art because those files had fake checkerboard backgrounds.
- Replaced custom prop references with transparent CraftPix/imported assets.
- Rebuilt Sector 01 into a more open outdoor layout instead of the maze-style test map.
- Removed dark square blocked-tile overlay so trees/rocks no longer appear on black boxes.
- Added save migration so old local saves reset to the fixed Sector 01 map layout.
- Updated boot text to `Version 0.5.1 // MAP ART FIX`.
